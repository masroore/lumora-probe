# Example Plugin

Minimal analyzer using only the public Lumora Probe SDK.

```console
lumora plugins install examples/plugins/example_plugin --data-dir "$LUMORA_DATA_DIR"
```

Review trusted-code behavior before enabling. Enablement is restart-scoped; there is no API
installation endpoint.
