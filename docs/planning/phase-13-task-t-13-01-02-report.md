# Phase 13 Task Report — T-13-01-02 Sidecar metadata

**Status:** Complete

`DecodedFrameMetadata` exposes dimensions, frame identity/count, rescale slope/intercept, suggested window, photometric interpretation, and transfer syntax. Sidecar serialization is stable JSON-compatible data.
