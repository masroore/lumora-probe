# Phase 13 Task Report — T-13-02-01 frame endpoints

**Status:** Complete

Added per-instance/per-frame raw uint16 little-endian and JSON sidecar endpoints. The endpoint never re-encodes normalized pixels as PNG/JPEG and exposes decode duration headers.
