# Phase 13 Task Report — T-13-03-03 partial-study marking

**Status:** Complete

Provenance exposes `present_in_capture_count`; the existing projection `partial` flag remains derived from capture count. The browser data model does not silently union capture-scoped instances.
