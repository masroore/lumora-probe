# Phase 13 Task Report — T-13-03-02 per-instance provenance

**Status:** Complete

`StudyBrowserService.provenance()` groups instance rows by SOP Instance UID and preserves sorted capture IDs and object digests.
