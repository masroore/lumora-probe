interface:
  display_name: "Lumora Probe"
  short_description: "Repo-specific patterns and workflows for lumora-probe"
  default_prompt: "Use the lumora-probe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true